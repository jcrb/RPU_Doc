% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Rapport.R
\name{barplot.week.variations}
\alias{barplot.week.variations}
\title{Variation du nombre de RPU par semaine}
\usage{
barplot.week.variations()
}
\arguments{
\item{x}{vecteur du nombre de RPU pr semaine (voir week.rpu)}

\item{coltitre}{bool, si TRUE la valeur de la barre est inscrite au dessus ou en dessous}

\item{colmoins}{couleur des barres négatives. Red par défaut}

\item{colplus}{couleur des barres positives. Blue par défaut}

\item{xlab}{nom pour l'axe des X. 'Semaines' par défaut}

\item{cex.names}{échelle pour le titre des barres (n° de la semaine). 0.8 par défaut}

\item{cex.col}{échelle pour les valeurs des colonnes. Utile que si coltitre = TRUE. Défaut 0.8}

\item{dx}{écart entre le sommet de la barre et l'affichage de sa valeur. Utile que si coltitre = TRUE. Défaut 3.}

\item{...}{autres paramètres pour boxplot}
}
\value{
le vecteur des abcisses des colonnes
}
\description{
Variation du nombre de RPU par semaine
}
\examples{
v <- week.variations(dx[dx$FINESS == "3Fr",])
barplot.week.variations(v[-length(v)], las = 2, main = "test", ylim = c(min(v[-length(v)])-10, max(v[-length(v)])+10), 
ylab = "Variations hebdomadaires")

###
v <- week.variations(week.rpu(dx[dx$FINESS == "Col",]))
barplot.week.variations(v[-length(v)], las = 2, main = "CH Colmar - 2015", 
ylim = c(min(v[-length(v)])-10, max(v[-length(v)])+10), ylab = "Variations hebdomadaires", dx = 5)

}

